#include <bits/stdc++.h>
using namespace std;

int dp[101010];

int main() {
	int n, m;
	cin >> n >> m;
	vector< int > x(m), y(m);
	for (int i=0; i<m; ++i) cin >> x[i] >> y[i];
	
	
	for (int mi=0; mi<m; ++mi) {
		
	}
	
  return 0;
}
